# ローカル仮想環境（リポジトリ外で venv を使う場合は不要）
venv/

# 実行時ログ（ルート .gitignore の logs/ と併用）
logs/

# 開発用 TLS（start.py 生成。ルート .gitignore と同様）
certs/dev-lan.*

# ローカル環境変数
.env

# Device Owner QR 配布用 APK（アップロード成果物）
data/device_owner/

# Python キャッシュ
__pycache__/
*.py[cod]
